# Normal rolling regression

This is an internal function.

## Usage

``` r
static_roll(df, win)
```

## Arguments

- df:

  data.frame object.

- win:

  numeric. width in number of rows

## Value

a data.table object
