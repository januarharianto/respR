# Prints the density object for summary.auto_rate S3 Basically copied from stats:::print.density and edited to make it more compact

Prints the density object for summary.auto_rate S3 Basically copied from
stats:::print.density and edited to make it more compact

## Usage

``` r
print_dens(x, digits = NULL, ...)
```

## Arguments

- x:

  auto_rate object.

- digits:

  numeric.

## Value

Console output
